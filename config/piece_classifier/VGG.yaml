DATASET:
  TRANSFORMS:
    CENTER_CROP: null
    RESIZE:
    - 100
    - 200
TRAINING:
  MODEL:
    NAME: VGG
    REGISTRY: PIECE_CLASSIFIER
  PHASES:
  - EPOCHS: 1
    OPTIMIZER:
      LEARNING_RATE: 0.001
      NAME: Adam
    PARAMS: head
  - EPOCHS: 2
    OPTIMIZER:
      LEARNING_RATE: 0.0001
      NAME: Adam
    PARAMS: all
_BASE_: config://piece_classifier/_base.yaml
