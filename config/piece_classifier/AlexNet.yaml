DATASET:
  TRANSFORMS:
    CENTER_CROP: null
    RESIZE:
    - 100
    - 200
TRAINING:
  MODEL:
    NAME: AlexNet
    REGISTRY: PIECE_CLASSIFIER
_BASE_: config://piece_classifier/_base_pretrained.yaml
